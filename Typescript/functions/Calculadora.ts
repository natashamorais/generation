import readline = require("readline-sync");

let num1: number;
let num2: number;

num1 = readline.questionFloat(" Digite o primeiro numero: ");
num1 = readline.questionFloat(" Digite o segundo numero: ");

function somar(num1: number, num2: number): number {
  return num1 + num2;
}

function subtrair(num1: number, num2: number): number {
  return num1 - num2;
}
function multiplicar(num1: number, num2: number): number {
  return num1 * num2;
}

function dividir( num1: number, num2: number): number| null{
    let divisao = dividir(numle)
}
function about(): void {
  console.log("PALESTINA LIVRE!");
}
3